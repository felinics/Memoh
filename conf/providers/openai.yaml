name: OpenAI
client_type: openai-responses
icon: openai
base_url: https://api.openai.com/v1

models:
  - model_id: gpt-6-astra
    name: GPT-6 Astra
    type: chat
    config:
      description: Most capable GPT-6 model for complex reasoning and end-to-end work.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [low, medium, high, xhigh, max]

  - model_id: gpt-6-sol
    name: GPT-6 Sol
    type: chat
    config:
      description: GPT-6 model for complex coding and agentic workflows.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [disable, low, medium, high, xhigh, max]

  - model_id: gpt-6-luna
    name: GPT-6 Luna
    type: chat
    config:
      description: Efficient GPT-6 model for focused, high-volume tasks.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [disable, low, medium, high, xhigh, max]

  - model_id: gpt-5.6-sol
    name: GPT-5.6 Sol
    type: chat
    config:
      description: Flagship GPT-5.6 model for complex professional work.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [disable, low, medium, high, xhigh, max]

  - model_id: gpt-5.6-terra
    name: GPT-5.6 Terra
    type: chat
    config:
      description: Balanced GPT-5.6 model for intelligence and cost.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [disable, low, medium, high, xhigh, max]

  - model_id: gpt-5.6-luna
    name: GPT-5.6 Luna
    type: chat
    config:
      description: Efficient GPT-5.6 model for high-volume workloads.
      compatibilities: [vision, file-input, tool-call, reasoning]
      context_window: 1050000
      thinking_mode: toggle
      reasoning_efforts: [disable, low, medium, high, xhigh, max]

  - model_id: text-embedding-3-large
    name: Text Embedding 3 Large
    type: embedding
    config:
      dimensions: 3072

  - model_id: text-embedding-3-small
    name: Text Embedding 3 Small
    type: embedding
    config:
      dimensions: 1536
