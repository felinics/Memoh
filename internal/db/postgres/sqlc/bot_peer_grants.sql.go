// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: bot_peer_grants.sql

package sqlc

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const createBotPeerGrant = `-- name: CreateBotPeerGrant :one
INSERT INTO bot_peer_grants (bot_id, subject_type, subject_bot_id, permissions, created_by_user_id)
VALUES (
  $1,
  $2,
  $4::uuid,
  $3,
  $5::uuid
)
RETURNING id, bot_id, subject_type, subject_bot_id, permissions, created_by_user_id, created_at, updated_at, team_id
`

type CreateBotPeerGrantParams struct {
	BotID           pgtype.UUID `json:"bot_id"`
	SubjectType     string      `json:"subject_type"`
	Permissions     []byte      `json:"permissions"`
	SubjectBotID    pgtype.UUID `json:"subject_bot_id"`
	CreatedByUserID pgtype.UUID `json:"created_by_user_id"`
}

type CreateBotPeerGrantRow struct {
	ID              pgtype.UUID        `json:"id"`
	BotID           pgtype.UUID        `json:"bot_id"`
	SubjectType     string             `json:"subject_type"`
	SubjectBotID    pgtype.UUID        `json:"subject_bot_id"`
	Permissions     []byte             `json:"permissions"`
	CreatedByUserID pgtype.UUID        `json:"created_by_user_id"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
	TeamID          pgtype.UUID        `json:"team_id"`
}

func (q *Queries) CreateBotPeerGrant(ctx context.Context, arg CreateBotPeerGrantParams) (CreateBotPeerGrantRow, error) {
	row := q.db.QueryRow(ctx, createBotPeerGrant,
		arg.BotID,
		arg.SubjectType,
		arg.Permissions,
		arg.SubjectBotID,
		arg.CreatedByUserID,
	)
	var i CreateBotPeerGrantRow
	err := row.Scan(
		&i.ID,
		&i.BotID,
		&i.SubjectType,
		&i.SubjectBotID,
		&i.Permissions,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.TeamID,
	)
	return i, err
}

const deleteBotPeerGrantByID = `-- name: DeleteBotPeerGrantByID :exec
DELETE FROM bot_peer_grants WHERE team_id = public.memoh_current_team_id() AND id = $1
`

func (q *Queries) DeleteBotPeerGrantByID(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteBotPeerGrantByID, id)
	return err
}

const getBotPeerGrantByID = `-- name: GetBotPeerGrantByID :one
SELECT id, bot_id, subject_type, subject_bot_id, permissions, created_by_user_id, created_at, updated_at, team_id
FROM bot_peer_grants
WHERE team_id = public.memoh_current_team_id() AND id = $1
`

type GetBotPeerGrantByIDRow struct {
	ID              pgtype.UUID        `json:"id"`
	BotID           pgtype.UUID        `json:"bot_id"`
	SubjectType     string             `json:"subject_type"`
	SubjectBotID    pgtype.UUID        `json:"subject_bot_id"`
	Permissions     []byte             `json:"permissions"`
	CreatedByUserID pgtype.UUID        `json:"created_by_user_id"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
	TeamID          pgtype.UUID        `json:"team_id"`
}

func (q *Queries) GetBotPeerGrantByID(ctx context.Context, id pgtype.UUID) (GetBotPeerGrantByIDRow, error) {
	row := q.db.QueryRow(ctx, getBotPeerGrantByID, id)
	var i GetBotPeerGrantByIDRow
	err := row.Scan(
		&i.ID,
		&i.BotID,
		&i.SubjectType,
		&i.SubjectBotID,
		&i.Permissions,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.TeamID,
	)
	return i, err
}

const listBotPeerGrants = `-- name: ListBotPeerGrants :many
SELECT
  g.id,
  g.bot_id,
  g.subject_type,
  g.subject_bot_id,
  g.permissions,
  g.created_by_user_id,
  g.created_at,
  g.updated_at,
  s.name AS subject_bot_name,
  s.display_name AS subject_bot_display_name,
  s.avatar_url AS subject_bot_avatar_url
FROM bot_peer_grants g
LEFT JOIN bots s
  ON s.team_id = g.team_id
 AND s.id = g.subject_bot_id
WHERE g.team_id = public.memoh_current_team_id() AND g.bot_id = $1
ORDER BY g.subject_type DESC, g.created_at ASC
`

type ListBotPeerGrantsRow struct {
	ID                    pgtype.UUID        `json:"id"`
	BotID                 pgtype.UUID        `json:"bot_id"`
	SubjectType           string             `json:"subject_type"`
	SubjectBotID          pgtype.UUID        `json:"subject_bot_id"`
	Permissions           []byte             `json:"permissions"`
	CreatedByUserID       pgtype.UUID        `json:"created_by_user_id"`
	CreatedAt             pgtype.Timestamptz `json:"created_at"`
	UpdatedAt             pgtype.Timestamptz `json:"updated_at"`
	SubjectBotName        pgtype.Text        `json:"subject_bot_name"`
	SubjectBotDisplayName pgtype.Text        `json:"subject_bot_display_name"`
	SubjectBotAvatarUrl   pgtype.Text        `json:"subject_bot_avatar_url"`
}

func (q *Queries) ListBotPeerGrants(ctx context.Context, botID pgtype.UUID) ([]ListBotPeerGrantsRow, error) {
	rows, err := q.db.Query(ctx, listBotPeerGrants, botID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListBotPeerGrantsRow
	for rows.Next() {
		var i ListBotPeerGrantsRow
		if err := rows.Scan(
			&i.ID,
			&i.BotID,
			&i.SubjectType,
			&i.SubjectBotID,
			&i.Permissions,
			&i.CreatedByUserID,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.SubjectBotName,
			&i.SubjectBotDisplayName,
			&i.SubjectBotAvatarUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBotPeerGrantsForCaller = `-- name: ListBotPeerGrantsForCaller :many
SELECT id, bot_id, subject_type, subject_bot_id, permissions
FROM bot_peer_grants
WHERE team_id = public.memoh_current_team_id()
  AND bot_id = $1
  AND (
    (subject_type = 'any_bot' AND bot_id <> $2::uuid)
    OR (subject_type = 'bot' AND subject_bot_id = $2::uuid)
  )
`

type ListBotPeerGrantsForCallerParams struct {
	BotID       pgtype.UUID `json:"bot_id"`
	CallerBotID pgtype.UUID `json:"caller_bot_id"`
}

type ListBotPeerGrantsForCallerRow struct {
	ID           pgtype.UUID `json:"id"`
	BotID        pgtype.UUID `json:"bot_id"`
	SubjectType  string      `json:"subject_type"`
	SubjectBotID pgtype.UUID `json:"subject_bot_id"`
	Permissions  []byte      `json:"permissions"`
}

// Callee-side resolution: every grant on bot $1 that applies to caller bot $2.
// The any_bot row never applies to the callee itself, so a bot can never reach
// itself through a blanket grant.
func (q *Queries) ListBotPeerGrantsForCaller(ctx context.Context, arg ListBotPeerGrantsForCallerParams) ([]ListBotPeerGrantsForCallerRow, error) {
	rows, err := q.db.Query(ctx, listBotPeerGrantsForCaller, arg.BotID, arg.CallerBotID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListBotPeerGrantsForCallerRow
	for rows.Next() {
		var i ListBotPeerGrantsForCallerRow
		if err := rows.Scan(
			&i.ID,
			&i.BotID,
			&i.SubjectType,
			&i.SubjectBotID,
			&i.Permissions,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBotPeerGrantsForSubject = `-- name: ListBotPeerGrantsForSubject :many
SELECT
  g.id,
  g.bot_id,
  g.subject_type,
  g.subject_bot_id,
  g.permissions,
  b.name AS bot_name,
  b.display_name AS bot_display_name,
  b.avatar_url AS bot_avatar_url
FROM bot_peer_grants g
INNER JOIN bots b
  ON b.team_id = g.team_id
 AND b.id = g.bot_id
WHERE g.team_id = public.memoh_current_team_id()
  AND (
    (g.subject_type = 'any_bot' AND g.bot_id <> $1::uuid)
    OR (g.subject_type = 'bot' AND g.subject_bot_id = $1::uuid)
  )
ORDER BY b.created_at ASC
`

type ListBotPeerGrantsForSubjectRow struct {
	ID             pgtype.UUID `json:"id"`
	BotID          pgtype.UUID `json:"bot_id"`
	SubjectType    string      `json:"subject_type"`
	SubjectBotID   pgtype.UUID `json:"subject_bot_id"`
	Permissions    []byte      `json:"permissions"`
	BotName        string      `json:"bot_name"`
	BotDisplayName pgtype.Text `json:"bot_display_name"`
	BotAvatarUrl   pgtype.Text `json:"bot_avatar_url"`
}

// Caller-side reverse lookup: every bot that caller $1 may reach, with the
// callee's profile for display. Uses idx_bot_peer_grants_subject for the
// explicit edges and a sequential scan of the (small) any_bot set.
func (q *Queries) ListBotPeerGrantsForSubject(ctx context.Context, callerBotID pgtype.UUID) ([]ListBotPeerGrantsForSubjectRow, error) {
	rows, err := q.db.Query(ctx, listBotPeerGrantsForSubject, callerBotID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListBotPeerGrantsForSubjectRow
	for rows.Next() {
		var i ListBotPeerGrantsForSubjectRow
		if err := rows.Scan(
			&i.ID,
			&i.BotID,
			&i.SubjectType,
			&i.SubjectBotID,
			&i.Permissions,
			&i.BotName,
			&i.BotDisplayName,
			&i.BotAvatarUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateBotPeerGrantPermissions = `-- name: UpdateBotPeerGrantPermissions :one
UPDATE bot_peer_grants
SET permissions = $2,
    updated_at = now()
WHERE team_id = public.memoh_current_team_id() AND id = $1
RETURNING id, bot_id, subject_type, subject_bot_id, permissions, created_by_user_id, created_at, updated_at, team_id
`

type UpdateBotPeerGrantPermissionsParams struct {
	ID          pgtype.UUID `json:"id"`
	Permissions []byte      `json:"permissions"`
}

type UpdateBotPeerGrantPermissionsRow struct {
	ID              pgtype.UUID        `json:"id"`
	BotID           pgtype.UUID        `json:"bot_id"`
	SubjectType     string             `json:"subject_type"`
	SubjectBotID    pgtype.UUID        `json:"subject_bot_id"`
	Permissions     []byte             `json:"permissions"`
	CreatedByUserID pgtype.UUID        `json:"created_by_user_id"`
	CreatedAt       pgtype.Timestamptz `json:"created_at"`
	UpdatedAt       pgtype.Timestamptz `json:"updated_at"`
	TeamID          pgtype.UUID        `json:"team_id"`
}

func (q *Queries) UpdateBotPeerGrantPermissions(ctx context.Context, arg UpdateBotPeerGrantPermissionsParams) (UpdateBotPeerGrantPermissionsRow, error) {
	row := q.db.QueryRow(ctx, updateBotPeerGrantPermissions, arg.ID, arg.Permissions)
	var i UpdateBotPeerGrantPermissionsRow
	err := row.Scan(
		&i.ID,
		&i.BotID,
		&i.SubjectType,
		&i.SubjectBotID,
		&i.Permissions,
		&i.CreatedByUserID,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.TeamID,
	)
	return i, err
}
